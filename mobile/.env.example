# Base URL of the Kifaah backend API (see ../backend/.env.example for server-side config,
# including the Razorpay test-mode keys the WebView checkout in PaymentScreen needs).
EXPO_PUBLIC_API_URL=http://localhost:4000
